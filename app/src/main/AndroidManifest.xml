<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sn77.crio.criofashion.date20200621954">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddressAddingActivity"></activity>
        <activity android:name=".UserDetailsActivity" />
        <activity android:name=".SettingsMenuActivity" />
        <activity android:name=".CreateAnAccountActivity" />
        <activity android:name=".UserLoginActivity" />
        <activity android:name=".MainActionPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>